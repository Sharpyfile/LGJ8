
public enum SpectatorState
{
    ENTERING,
    WATCHING,
    EXITING
}

public enum CardState
{
    CLICKED,
    NOT_CLICKED,
    HOVERED,
    NOT_HOVERED
}

public enum CardAnimationState
{
    BEGIN_SLIDE,
    DURING_SLIDE,
    END_SLIDE,
}