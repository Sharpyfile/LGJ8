
public enum SpectatorState
{
    ENTERING,
    WATCHING,
    EXITING
}

public enum CardState
{
    CLICKED,
    NOT_CLICKED,
    HOVERED,
    NOT_HOVERED
}

public enum CardAnimationState
{
    IDLE,
    SlIDE_IN,
    TOP,
    SLIDE_OUT
}